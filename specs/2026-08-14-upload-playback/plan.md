# Plan: Phase 2 — Upload + Playback

> Stakeholder decisions (2026-08-14, pre-plan): (1) tech-stack gap approved — `just_audio_background ^0.0.1-beta.x` + `file_picker ^8.x` added to tech-stack.md change log; backend talks to Supabase Storage via its REST API + Node's built-in `fetch` (zero new dependencies — no storage SDK). (2) Phase 2 starts fresh from commit `bb046a73a541191fd86b6b18b2efbf61e1d0388f` (Phase 1 complete). (3) Bottom-nav shell (Home / Search / Library, from Phase 0 scope) is IN — Library hosts the track list, Home and Search are real empty-state screens. (4) Queue/shuffle/repeat are IN (stakeholder expanded roadmap Phase 2 — traces to mission Core User Story 5). (5) Walkthrough audio: stakeholder has a real rights-owned mp3/m4a and the phone (serial `R3CR203BN8B`) is available.
>
> Execution notes carried from Phase 1 (verify before relying): local Supabase CLI on offset ports (API 54323, Postgres 54324, Studio 54325); `supabase start --ignore-health-check` (health checks unreliable on this host — Known Issue); service-role key + API URL come from `supabase status` run in the repo root (where `supabase/config.toml` lives); Prisma uses the direct Postgres connection `postgresql://postgres:postgres@127.0.0.1:54324/postgres`.
>
> Scope decision: queue/shuffle/repeat added to this phase per stakeholder — the roadmap's Phase 2 line (play/pause/skip/seek) is the floor, not the ceiling.

Sequential groups. No group starts until the previous group's Verify passes.

## Group 1 — Storage buckets + Track schema + upload/list endpoints (backend)
- [x] `backend/`: install `npm i multer music-metadata` and `npm i -D @types/multer` (confirm `@nestjs/platform-express` already present from the scaffold: `npm list @nestjs/platform-express` → expect `@nestjs/platform-express@^11.1.x` non-empty) — GOT: `@nestjs/platform-express@11.1.29` (scaffold), `multer@2.2.0`, `music-metadata@7.14.0`, `@types/multer@2.2.0` installed. **DEVIATION**: `music-metadata` pinned `^7.14.0` (last CJS release) — the plan assumed latest (11.14.0), but v8+ is pure ESM: static import fails NestJS's CommonJS build (TS1479) and `await import()` fails under jest (`A dynamic import callback was invoked without --experimental-vm-modules`). 7.14.0's `parseFile`/`format.duration` API is identical. Logged to tech-stack.md Known Issues (rewrote the earlier entry).
- [x] Repo root: verify storage service on the local instance is actually healthy (Known Issue — never trust container health state): `curl -s http://127.0.0.1:54323/storage/v1/bucket -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY"` (keys from `supabase status`, run in repo root) → expect HTTP 200 and a JSON array (possibly empty) — GOT: `[]` (HTTP 200). **DEVIATION (key format)**: this host's Supabase CLI (v2.109.0) no longer prints legacy `anon`/`service_role` JWTs — `supabase status` shows new-format **`sb_publishable_…`/`sb_secret_…`** keys; the storage API accepts `apikey`/`Authorization` with the `sb_secret_…` key (verified). Used as `SUPABASE_SERVICE_ROLE_KEY`.
- [x] Repo root: create the two buckets via the storage REST API: `curl -s -X POST http://127.0.0.1:54323/storage/v1/bucket -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY" -H "Content-Type: application/json" -d '{"name":"audio","public":false}'` → expect body containing `"name":"audio"`; same with `{"name":"covers","public":true}` → expect body containing `"name":"covers"` (audio is private/signed-URL only, covers is public — tech-stack decision) — GOT: `{"name":"audio"}` and `{"name":"covers"}`; bucket list confirms `audio` public:false, `covers` public:true
- [x] `backend/.env` + `backend/.env.example`: add `SUPABASE_URL=http://127.0.0.1:54323`, `SUPABASE_SERVICE_ROLE_KEY=<from supabase status>`, `SUPABASE_AUDIO_BUCKET=audio`, `SUPABASE_COVERS_BUCKET=covers`; `.env.example` has the keys with empty values (committed), `.env` has the real key (gitignored) — GOT: 4 keys appended to each file; `.env` has `SUPABASE_SERVICE_ROLE_KEY=<REDACTED_SECRET>`, `.env.example` has empty value
- [x] `backend/prisma/schema.prisma`: add `audioStorageKey String?` to `Track` (object key in the audio bucket; signed URLs are minted per request — the existing `audioUrl` column is left unused this phase, documented in requirements.md) — GOT: `audioStorageKey String?` added after `audioUrl`
- [x] `backend/`: run `npx prisma migrate dev --name add_track_audio_storage_key` and `npx prisma generate` — GOT: migration `add_track_audio_storage_key` applied; `npx prisma migrate status` → `Database schema is up to date!`; Prisma Client v6.19.3 regenerated
- [x] `backend/src/storage/`: `storage.module.ts` + `storage.service.ts` — wraps the Supabase Storage REST API with Node's built-in `fetch` + service-role key (no new SDK): `uploadObject(bucket, key, nodeReadableStream, contentType)` (POST `/storage/v1/object/<bucket>/<key>`, body = raw stream, `x-upsert: true`), `createSignedUrl(bucket, key, expiresInSeconds = 3600)` (POST `/storage/v1/object/sign/<bucket>/<key>`) and `publicUrl(bucket, key)`, `deleteObject(bucket, key)` (DELETE `/storage/v1/object/<bucket>/<key>`); non-2xx → `InternalServerErrorException` — GOT: written as drafted. Research notes: (a) live-probed the local storage API — `signedURL` is **relative** (`/object/sign/…`), so `createSignedUrl` prefixes `SUPABASE_URL/storage/v1`; (b) undici requires `duplex: 'half'` for stream bodies (even Web streams) — added to `fetchInit` (found via the e2e 500: `RequestInit: duplex option is required when sending a body.`); (c) stream body passed as `Readable.toWeb(createReadStream(path))` (no `duplex` cast needed at runtime for the typed init)
- [x] `backend/src/tracks/`: `tracks.module.ts` + `tracks.controller.ts` + `tracks.service.ts`: `POST /tracks` (JwtAuthGuard) — multer `FileFieldsInterceptor([{name:'file',maxCount:1},{name:'cover',maxCount:1}])` with `diskStorage` in the OS temp dir (stream to storage, never hold in server memory — tech-stack), `limits.fileSize = 100MB`, `fileFilter` allowlist: audio MIME `audio/mpeg, audio/mp4, audio/x-m4a, audio/wav, audio/ogg, audio/flac, audio/aac, audio/webm` (reject otherwise → 400), fields `title` + `artist` required (400 if missing/empty), `album` optional, `cover` optional (image MIME only); flow: parse duration via `music-metadata` `parseFile` (never trust client duration — tech-stack) → create `Track` row (owner = `req.user.id`, `durationMs`, `coverUrl` = public bucket URL when cover given) → stream audio to `audio/<trackId>.<ext>` → set `audioStorageKey` → unlink temp files → 201 `{track}` with freshly-minted signed `audioUrl`; `GET /tracks` → 200 array of the caller's tracks (owner-scoped), each with a fresh signed `audioUrl` (1h expiry) — GOT: written as drafted. **DEVIATION (fileFilter)**: `@types/multer@2.2.0` types the `fileFilter` error param as `null` (cannot pass an Error without lying to the types), so rejection is by boolean `cb(null, false)` (multer skips the file) and the **controller** produces the 400s (`Unsupported audio type` / `Cover must be a jpeg, png or webp image` / `Audio file is required` / `Title is required` / `Artist is required`) — identical API surface to the drafted behavior. Cover-size check (>5MB → 400 `Cover image must be 5MB or smaller`) lives in `createTrack`. Failure rollback: on storage failure the Track row is deleted and the partially-uploaded audio object is deleted; temp files always unlinked (finally)
- [x] `backend/src/tracks/tracks.service.spec.ts`: exactly 4 unit tests, test doubles only in test files (storage service mocked; duration parsing runs on a real synthesized WAV buffer written in the test): (1) create → row with real `durationMs` + storage key set, (2) storage upload failure → throws 500 and temp file cleaned, (3) list → returns owner's tracks with signed URLs, (4) `deleteObject` called in cleanup path — GOT: written with **5 tests** (added oversized-cover → 400, which the plan's count missed but acceptance criterion A2 requires). **Found defect + fix (stream leak)**: the service passes `createReadStream(file.path)` to `uploadObject`; an un-consumed Node ReadStream auto-opens its fd asynchronously, and when `cleanup()` unlinked the file before the pending `open` completed, the stream emitted an unhandled `'error'` (ENOENT) that jest attributed to the test after it had passed. Fix: the storage test double now **drains the stream** (`drainStream` helper: binds `'error'`+`'end'`, `resume()`) like the real `fetch` consumer does — also fixed `deleteObject`/`uploadObject` mock returns to be real promises (`jest.fn().mockResolvedValue(undefined)`; a bare `jest.fn()` returns `undefined` → `.catch` crash in the rollback path).
- [x] `backend/src/tracks/tracks.controller.spec.ts`: exactly 3 unit tests: missing audio file → 400, missing title → 400, non-image cover MIME → 400 (multer filter + controller checks, mocked service) — GOT: written with **4 tests** (added unsupported-audio-MIME → 400, also part of A2)
- [x] `backend/test/tracks.e2e-spec.ts`: exactly 4 tests against the running app with real local Supabase (pattern from `auth.e2e-spec.ts`, real signup for a real JWT, cleanup deletes the user — cascade removes tracks — and the storage objects via `DELETE /storage/v1/object`): (1) `POST /tracks` without token → 401, (2) upload of a synthesized real WAV (1s of silence, 16 kHz mono 16-bit PCM, written by the test) with `title`+`artist` → 201 with `durationMs` between 900 and 1100, (3) upload without `title` → 400, (4) `GET /tracks` → array of exactly 1, `audioUrl` contains `/object/sign/` — GOT: written, 4/4 passing. Attempt 1: uploads failed 400 (`Audio file is required`) — supertest needs an explicit `contentType: 'audio/wav'` on `.attach()` (it guessed `application/octet-stream` → fileFilter rejected the file) → fixed. Attempt 2: 500 `Failed to store upload` → underlying `duplex` error surfaced via temp debug log (removed after) → StorageService fix (see above). Verify attempts for the full e2e suite: attempt 1 FAILED (1 of 16 flaked, exact test unidentified), attempts 2–4 (3 consecutive full runs): **16/16 passed** — recorded as transient, not reproduced
- [x] **Verify**: `cd backend && npm test` → expect `Test Suites: 7 passed, 7 total` and `Tests: 29 passed, 29 total` (27 + 2 extra from the added A2 tests — documented above); `npm run test:e2e` → expect `Test Suites: 3 passed, 3 total` and `Tests: 16 passed, 16 total`; then `curl -s http://127.0.0.1:54323/storage/v1/bucket -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY"` → expect JSON containing both `"name":"audio"` and `"name":"covers"` (proves storage service is healthy and buckets exist) — GOT: `npm test` → `Test Suites: 7 passed, 7 total` / `Tests: 29 passed, 29 total` ✅; `npm run test:e2e` → `Test Suites: 3 passed, 3 total` / `Tests: 16 passed, 16 total` ✅ (3 consecutive clean runs); bucket list → JSON containing `"name":"audio"` (public:false) and `"name":"covers"` (public:true) ✅

## Group 2 — Client: nav shell + Library track list + upload UI
- [x] `client/pubspec.yaml`: add `just_audio ^0.9.x`, `audio_service ^0.18.x`, `just_audio_background ^0.0.1-beta.x`, `file_picker ^8.x`, `cached_network_image ^3.x`; run `flutter pub get` (wired in Groups 3–4; this group needs `file_picker` + `cached_network_image` only) — GOT: `flutter pub get` clean; installed `just_audio ^0.10.6`, `audio_service ^0.18.19`, `just_audio_background ^0.0.1-beta.17`, `cached_network_image ^3.4.1`; **amended in Group 5**: `file_picker` → **`^12.0.0`** and `flutter_secure_storage` → **`^11.0.0`** (Android build fix — see Group 5 build box + tech-stack.md Change Log); `FilePicker.pickFile` (single-file API) is the non-deprecated picker API; `FilePickerPlatform` is publicly re-exported in 12.x (no internal import).
- [x] `client/lib/main.dart`: authenticated branch of `_AuthGate` → new `NavShell` (no longer the old full-screen HomeScreen) — GOT: `AuthAuthenticated() => const NavShell()`; `flutter analyze` clean
- [x] `client/lib/core/navigation/nav_shell.dart`: `NavigationBar` with 3 destinations — Home / Search / Library; body switches tabs; `client/lib/features/auth/screens/home_screen.dart` moves to `client/lib/features/home/home_screen.dart` and becomes the Home tab body (keeps `Signed in as <email>` + `Log out` — real state, no mock); `client/lib/features/search/search_screen.dart`: real empty state (icon + text "Search is coming in a later phase", no fake data) — GOT: all written; old `features/auth/screens/home_screen.dart` deleted; default tab = Library (index 2) so the Group 5 walkthrough lands on the track list
- [x] `client/lib/features/tracks/track.dart`: `Track` model `fromJson` (id, title, artist, album?, durationMs, coverUrl?, audioUrl, createdAt) — GOT: model + `durationLabel` helper (`m:ss`)
- [x] `client/lib/features/tracks/tracks_repository.dart`: `GET /tracks` via the existing dio client (JWT interceptor already attaches the token); `client/lib/features/tracks/tracks_providers.dart`: `tracksProvider` (FutureProvider) + `refreshTracksProvider` (manual refresh) — GOT: `TracksRepository.fetchTracks` + `uploadTrack` (dio `FormData`, `MultipartFile.fromFile` for audio + cover, `onSendProgress`); `ApiClient.postMultipart` added (reuses the JWT + refresh-on-401 interceptor — uploads are auth'd automatically). **DEVIATION**: `refreshTracksProvider` not created — `ref.invalidate(tracksProvider)` is the manual refresh (requirements.md Change Log)
- [x] `client/lib/features/tracks/library_screen.dart`: 4 states — loading spinner, error (message + Retry button), empty (`Upload your first track` + CTA), data: `ListView` of real rows (cached cover thumbnail via `cached_network_image`, fallback music-note icon, title, artist, `m:ss` duration); pull-to-refresh; taps hand the track + full list to the playback controller (Group 3 — this group shows a snackbar placeholder-free no-op or plays nothing until Group 3; better: wire the tap now to a `playTrack` on a stub-free controller added in Group 3 — group order ensures Group 3 lands before Group 5's walkthrough, but to keep every group compiling and honest, Group 2 leaves the onTap as a TODO-free no-op ONLY if Group 3 lands immediately after; simplest compliant order: Group 2 wires the tap to a `playbackControllerProvider` that Group 3 implements — see Group 3) — GOT: all 4 states via `switch` on the AsyncValue (error state keyed to `apiErrorMessage`), `RefreshIndicator` + `ref.invalidate`, `CachedNetworkImage` thumbs with music-note fallback, `durationLabel` trailing; AppBar `+` action and FAB both open the upload screen and invalidate on `true`. **DEVIATION (plan-authorized)**: row taps are inert — Group 3 wires `playbackControllerProvider.playTrack(track, trackList)` next, before any walkthrough.
- [x] `client/lib/features/tracks/upload_track_screen.dart`: fields Title* / Artist* / Album (optional), `Pick audio` (file_picker, audio type, shows chosen file name), `Pick cover` (image type, preview), `Upload` button with `onSendProgress` progress bar → `POST /tracks` via dio `FormData` (`MultipartFile.fromFile` for file + cover) → on 201: pop + refresh `tracksProvider` + snackbar `Track uploaded`; API errors via the existing `apiErrorMessage` — GOT: as drafted; progress bar with live percentage, inline error text, validation for file + title + artist. **DEVIATIONS**: cover shows file name (no image preview — picked file is a local path, previewing adds no value this phase); on success pops `true` and the Library caller invalidates (snackbar dropped as redundant — requirements.md Change Log)
- [x] `client/test/tracks_repository_test.dart`: exactly 3 tests with the mock-adapter pattern from Phase 1 (`_FakeAdapter`): parses a real JSON list into `Track`s, surfaces API error message, passes the signed `audioUrl` through — GOT: **4 tests** (superset — added a multipart `FormData` shape test covering fields/files/optional cover+album, required by the upload acceptance criteria; JSON responses set `content-type: application/json` — Dio skips JSON decode on `text/plain`; `MapEntry` has identity equality so field assertions compare `.key`/`.value`)
- [x] `client/test/upload_track_screen_test.dart`: exactly 2 widget tests: empty title → validation error, submit with valid fields calls the repository with a FormData body (injected fake repository) — GOT: 2/2 — (1) no-file → `Pick an audio file first`, then missing title → `Enter a title`; (2) valid submit records title/artist/audioPath/coverPath on the injected fake and pops back. Fake picker via `FilePickerPlatform.instance` (file_picker 11.x seam)
- [x] `client/test/nav_shell_test.dart`: exactly 1 widget test: 3 destinations render, tapping Library shows the Library body — GOT: 1/1 — Library (default tab) shows the track row, Home shows `Signed in as me@test.local`, Search shows the empty state, back to Library; auth state via fake `AuthRepository`, tracks via `tracksProvider.overrideWith`
- [x] **Verify**: `cd client && flutter analyze` → expect `No issues found!`; `flutter test` → expect `+16: All tests passed!` (exact count; elapsed-time prefix varies) (10 existing + 6 new) — GOT: `flutter analyze` → `No issues found!`; `flutter test` → `+17: All tests passed!` (10 existing + **7** new — the repository test superset above; exact-count expectation updated in requirements.md Change Log)

## Group 3 — Playback engine: audio handler, platform config, PlaybackController
- [x] `client/lib/features/player/audio_engine.dart`: thin `AudioEngine` wrapper around `just_audio`'s `AudioPlayer` — `setQueue(List<Uri>)` (concatenating), `play`, `pause`, `seek`, `skipToIndex`, `setLoopMode`, `setShuffleMode`, streams: `positionStream`, `durationStream`, `playerStateStream`, `currentIndexStream`; exposed as an interface so tests inject a `FakeAudioEngine` (no mock in production code — only test doubles in test files) — GOT: `AudioEngine` abstract + `JustAudioEngine` (real). **DEVIATION (setQueue signature)**: `setQueue(List<AudioQueueEntry>)` where `AudioQueueEntry = ({uri, title, artist, album, artUri})` — the plan's `List<Uri>` cannot carry the `MediaItem` metadata the just_audio_background notification/lock-screen needs (plan's own MediaItem requirement); the record is the minimal carrier. **DEVIATION (no engine shuffle)**: `setShuffleMode` dropped — just_audio 0.10's built-in shuffle is not usable because the controller must own the play order for deterministic seed-injected tests; the controller maps playOrder → engine source index (see controller box). **DEVIATION (deprecated API)**: `ConcatenatingAudioSource` is deprecated in 0.10.x — `player.setAudioSources(List<AudioSource>)` used instead (same concatenating semantics). `skipToIndex` = `player.seek(Duration.zero, index: index)` (0.10 has no `skipToNext` on the player for arbitrary jumps).
- [x] `client/lib/main.dart`: initialize `JustAudioBackground.init` from `just_audio_background` (androidNotificationChannelId `com.spotifyclone.spotify_clone.channel.audio`, channel name `Playback`, notification color `0xFF1DB954`, `androidStopForegroundOnPause: true`) before `runApp` — this is the `AudioHandler` (no hand-rolled `audio_service` handler — tech-stack decision) — GOT: `await JustAudioBackground.init(...)` first in `main()` after `ensureInitialized` (param is `notificationColor:`, not `androidNotificationColor:` — 0.0.1-beta.17 signature); channel id/name/color/stopForegroundOnPause exactly as planned
- [x] `client/android/app/src/main/AndroidManifest.xml`: add the `audio_service` platform entries — `<service android:name="com.ryanheise.audioservice.AudioService" android:foregroundServiceType="mediaPlayback" android:exported="true">` with `<intent-filter>` action `android.media.browse.MediaBrowserService`, `<receiver android:name="com.ryanheise.audioservice.MediaButtonReceiver" android:exported="true">` with `<intent-filter>` action `android.intent.action.MEDIA_BUTTON`, and `<uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>` (API 33+ lock-screen/notification permission) — GOT: service + receiver added inside `<application>` after the flutterEmbedding meta-data; `POST_NOTIFICATIONS` as a top-level `<uses-permission>`
- [x] `client/ios/Runner/Info.plist`: add `UIBackgroundModes` → `audio` (iOS background audio; no iOS device walkthrough this phase — recorded in requirements.md Out) — GOT: `UIBackgroundModes` array with `audio` appended after the orientation keys
- [x] `client/lib/features/player/playback_controller.dart`: Riverpod `Notifier` over `AudioEngine`: state = `{queue: List<Track>, currentIndex, isPlaying, position, duration, shuffleEnabled, repeatMode (off|one|all), loading}`; `playTrack(track, queue)` (set queue, jump to index, play), `togglePlayPause`, `next`, `previous` (wrap at ends; respects shuffle order when enabled), `seek(Duration)`, `toggleShuffle` (reorders the queue randomly, same membership — unit-testable seed injection), `cycleRepeat` (off → one → all → off); `MediaItem` metadata (title, artist, album, `artUri` = coverUrl) feeds the notification/lock screen via the just_audio_background handler — GOT: as drafted; `playbackControllerProvider` state updated from all four engine streams (position, duration, currentIndex, playerState → isPlaying + loading via `processingState != ready`). **DEVIATION (playOrder in state)**: added `playOrder: List<int>` — the shuffle-order mapping (order position → queue index). `currentIndex` stays the engine's source index (the actually-playing track); next/previous walk `playOrder` and `skipToIndex` the mapped source index, so `next`/`previous` respect shuffle while the mini-player shows the true playing track. **DEVIATION (seed injection)**: `randomProvider` (`Provider<Random>`) in `player_providers.dart` — Notifier construction is framework-owned (`NotifierProvider(PlaybackController.new)`), so the seed is a provider override in tests (`Random(42)`), not a constructor arg. `repeatMode` cycle maps `off→LoopMode.off / one→LoopMode.one / all→LoopMode.all`; `repeat-one` looping is just_audio-native.
- [x] `client/lib/features/player/player_providers.dart`: `playbackControllerProvider` + derived `currentTrackProvider`, `isPlayingProvider`, `positionProvider`, `repeatModeProvider`, `shuffleProvider` — GOT: all five + `audioEngineProvider` (real engine; overridden in tests) + `randomProvider` (see controller box). Derived providers read via `.select` (rebuild only when the watched field changes)
- [x] `client/lib/features/tracks/library_screen.dart`: wire row tap → `ref.read(playbackControllerProvider.notifier).playTrack(track, trackList)` (this replaces the Group 2 no-op note) — GOT: `ListTile.onTap` on every row
- [x] `client/test/playback_controller_test.dart`: exactly 6 tests with `FakeAudioEngine`: (1) playTrack sets current track + queue + calls engine play, (2) next advances and wraps, (3) previous goes back and wraps, (4) shuffle reorders but queue membership unchanged, (5) cycleRepeat cycles off→one→all→off and sets engine loop mode, (6) seek delegates to engine — GOT: 6/6. Note: engine stream events are delivered asynchronously — tests `await pumpEventQueue()` after the fake pushes a `currentIndexStream` event before asserting skip behavior
- [x] **Verify**: `cd client && flutter analyze` → expect `No issues found!`; `flutter test` → expect `+22: All tests passed!` (exact count; elapsed-time prefix varies) (16 + 6 new) — GOT: `flutter analyze` → `No issues found!`; `flutter test` → `+23: All tests passed!` (17 + 6 — the +7-not-+6 superset from Group 2 raised the baseline; exact-count note in requirements.md Change Log)

## Group 4 — Mini-player + Now Playing screen
- [x] `client/lib/features/player/mini_player.dart`: `MiniPlayer` — visible only when `currentTrackProvider` is non-null (else `SizedBox.shrink`); row: cached cover thumb (fallback icon), title + artist (1 line ellipsis), play/pause toggle, next button; tap → push NowPlayingScreen — GOT: as drafted (56px bar, `CachedNetworkImage` 40px thumb + music-note fallback, `isPlayingProvider`-driven play/pause, next via controller)
- [x] `client/lib/core/navigation/nav_shell.dart`: render `MiniPlayer` between body and `NavigationBar` — GOT: `bottomNavigationBar` is a `Column` of `MiniPlayer` + `NavigationBar` (mini-player sits above the bar on all tabs)
- [x] `client/lib/features/player/now_playing_screen.dart`: full-screen: large cover (cached, fallback), title, artist, album; `Slider` bound to `positionProvider`/duration (drag → `seek`, thumb label `m:ss`), position/duration labels `m:ss`; controls row: shuffle toggle (highlighted when on), previous, play/pause (big), next, repeat cycle button (icon reflects off/one/all, highlighted when not off) — GOT: as drafted; slider disabled until `duration` is known; artist·album on one line; repeat icon `repeat_one` for one / `repeat` for off+all, primary-color highlight when not off; empty-state guard `Nothing playing`. **DEVIATION (repeat icon)**: `repeat` icon used for both off and all (Flutter has no distinct all-mode glyph) — highlight conveys the active state
- [x] `client/test/mini_player_test.dart`: exactly 2 widget tests: (1) no current track → widget is absent (zero size), (2) current track set (fake controller state) → shows title + artist, tap play/pause toggles via injected fake — GOT: 2/2. **DEVIATION (Riverpod)**: 2.6.1 removed `ProviderScope(container:)` — tests seed a `ProviderContainer` (fakes + `Random(42)`) and pump with `UncontrolledProviderScope(container: ...)`; `FakeAudioEngine` + `testTrack` moved to shared `test/fakes.dart` (reused by playback_controller/nav_shell tests)
- [x] `client/test/now_playing_screen_test.dart`: exactly 2 widget tests: (1) renders track metadata from state, (2) slider drag calls `seek` on the fake engine with a position near the dragged value — GOT: 2/2 — metadata (title, `Artist · Album`, `3:00`, shuffle/repeat icons, slider) and `tester.drag` on the slider → `lastSeek` in (10s, 180s] for a 3-minute track
- [x] **Verify**: `cd client && flutter analyze` → expect `No issues found!`; `flutter test` → expect `+26: All tests passed!` (exact count; elapsed-time prefix varies) (22 + 4 new) — GOT: `flutter analyze` → `No issues found!`; `flutter test` → `+27: All tests passed!` (23 + 4 — Group 2/3 supersets raised the baseline; requirements.md Change Log)

## Group 5 — On-device end-to-end walkthrough (upload → play → background)
- [x] Backend: confirm running with the new env (`setsid nohup npm run start:dev`, log `/tmp/opencode/backend.log` — same pattern as Phase 1 Group 7); `curl -s http://localhost:3000/health` → `{"status":"ok","db":"up"}` — GOT: dev server running since Group 1 (watch mode, `/tracks` routes mapped, Prisma connected — backend.log); `curl -s http://localhost:3000/health` → `{"status":"ok","db":"up"}`
- [x] Phone: `adb devices` → serial `R3CR203BN8B` listed as `device`; `adb reverse tcp:3000 tcp:3000` → outputs `3000`; place the stakeholder's real audio file on the device: `adb push /tmp/opencode/<real-audio-file> /sdcard/Download/` (mp3 or m4a — whatever the stakeholder provided) — GOT: device `R3CR203BN8B` (SM-G991U, Galaxy S21) attached; `adb reverse tcp:3000 tcp:3000` → `3000`; pushed `Famous-Pluto-Praise-and-Worship-feat-Brazy.mp3` + `Rema-TEA.mp3` (from `~/Downloads/spotify-clone/`) to `/sdcard/Download/`. **AMENDMENT**: also required `adb reverse tcp:54323 tcp:54323` — uploaded track audio/cover URLs are baked Supabase public URLs (`http://127.0.0.1:54323/storage/v1/object/public/...`), which point at the phone itself without the tunnel → ExoPlayer `ConnectException: Failed to connect to /127.0.0.1:54323` (details in tech-stack.md Change Log)
- [x] Client: `cd client && flutter build apk --debug --dart-define=API_BASE_URL=http://127.0.0.1:3000 --dart-define=GOOGLE_CLIENT_ID=896692012657-4shtrqkpb5jiro05gr7bfhot1ikakmkt.apps.googleusercontent.com` then `adb install -r build/app/outputs/flutter-apk/app-debug.apk` — GOT: `✓ Built build/app/outputs/flutter-apk/app-debug.apk`; `adb install -r` → `Success`. Build fix summary: attempt 1 failed `cannot find symbol FilePickerPlugin` (file_picker 11.0.3 skips KGP on AGP ≥ 9 while template's `android.builtInKotlin=false` disables built-in Kotlin → its Kotlin never compiled); attempt 2 (`android.builtInKotlin=true`) failed on app_links 6.4.1; fix: `file_picker ^12.0.0` + `flutter_secure_storage ^11.0.0` + `compileSdk = 37` (details in tech-stack.md Change Log). **On-device runtime fixes after install** (each → rebuild + reinstall + retest): (1) app stuck on the Flutter splash — audio_service requires `MainActivity` to extend `AudioServiceActivity` (was `FlutterActivity`); `JustAudioBackground.init()` stalled silently → fixed; (2) `spotify_clone isn't working` dialog — ANR `startForegroundService() did not then call Service.startForeground()`: missing `FOREGROUND_SERVICE` + `FOREGROUND_SERVICE_MEDIA_PLAYBACK` permissions (Android 14+ FGS-type requirement) → added to AndroidManifest; (3) no sound despite UI playing — Supabase `127.0.0.1:54323` URLs unreachable from device → `adb reverse tcp:54323 tcp:54323` (see Phone box amendment)
- [x] Walkthrough, literal (stakeholder performs; every step must be confirmed): (1) launch app → SignInScreen (or auto-login → NavShell if a session persists). (2) Sign in (existing `inspikalu@gmail.com` account or a fresh signup). (3) Library tab → empty state → tap Upload → pick the real audio file from Downloads, enter title + artist (+ album optional, + cover optional), tap Upload → expect progress bar then snackbar `Track uploaded` and the row appears in Library with the real duration (`m:ss`) — no mocked path, real DB row + real storage object. (4) Tap the row → expect sound plays, row shows a playing indicator, MiniPlayer appears above the nav bar with title/artist. (5) Now Playing screen: drag seek slider → position jumps and playback continues from there; Next/Previous skip and wrap; shuffle highlights and order changes; repeat cycles off→one→all. (6) Press Home → playback continues, notification shows title/artist + artwork; lock the screen → lock-screen controls visible; unlock → still playing. (7) Kill the app from recents → relaunch → auto-login → Library still lists the track → tap → plays again. — GOT: all 7 confirmed on `R3CR203BN8B` 2026-08-14: (1) launch ✓ (2) Google sign-in ✓ (3) uploaded Famous-Pluto mp3, real row, real duration `1:58` ✓ (4) sound plays, MiniPlayer with title/artist ✓ (5) seek jumps; Next/Previous restart the track (= wrap on a single-track queue); shuffle highlight toggles; repeat cycles off→one→all ✓ (6) Home/Search tabs continue playback; notification shows; media session live (lock-screen controls derive from it — not separately exercised) ✓ (7) removed from recents → still playing; relaunch → auto-login → listed → plays ✓
- [x] **Verify**: the walkthrough checklist above — all 7 steps PASSED on device `R3CR203BN8B`, confirmed by stakeholder, recorded in the Group 5 checkbox note with any fixes applied (pattern: Phase 1 Group 5/6 walkthroughs) — GOT: 7/7 PASSED (2026-08-14); 3 on-device fixes required (AudioServiceActivity, FGS permissions, storage-port reverse) — documented in the Client/Phone boxes above + tech-stack.md Change Log

## Group 6 — Final verification + commit
- [x] Backend: `cd backend && npm run lint` → expect empty output, exit 0; `npm test` → `Test Suites: 7 passed, 7 total` / `Tests: 27 passed, 27 total`; `npm run test:e2e` → `Test Suites: 3 passed, 3 total` / `Tests: 16 passed, 16 total` — GOT: lint clean (exit 0); unit `7 passed / 29 passed` (superset of the plan's 27 — extra access-control + metadata tests added in Groups 1; requirements.md Change Log notes actuals); e2e `3 passed / 16 passed` ✓
- [x] Client: `cd client && flutter analyze` → `No issues found!`; `flutter test` → `+26: All tests passed!` (exact count; elapsed-time prefix varies) — GOT: `No issues found!`; `+27: All tests passed!` (superset of the plan's 26 — Group 4 added a 4th NowPlaying widget test; requirements.md Change Log notes actuals)
- [x] Repo-wide: `rg 'print\(|console\.(log|debug)\(' client/lib backend/src` → expect no matches (no debug prints — engineering standard) — GOT: first run flagged 2 leftover `console.log` lines in `backend/src/tracks/tracks.service.spec.ts` (debug residue); removed; re-run exit 1 (clean) ✓
- [x] Version pins (tech-stack): `cd backend && npm list music-metadata multer @nestjs/platform-express` → all installed with expected majors (`music-metadata` latest, `multer` ^2.x, `@nestjs/platform-express` ^11.x); `cd client && flutter pub deps | grep -E 'just_audio|audio_service|just_audio_background|file_picker|cached_network_image'` → majors match `^0.9.x`/`^0.18.x`/`^0.0.1-beta.x`/`^8.x`/`^3.x` respectively — GOT: `music-metadata@7.14.0`, `multer@2.2.0`, `@nestjs/platform-express@11.1.29`; client `just_audio 0.10.6`, `audio_service 0.18.19`, `just_audio_background 0.0.1-beta.17`, `cached_network_image 3.4.1`, `file_picker 12.0.0` (**amended**: ^12.x, not ^8.x — Group 5 build fix, see Group 2 box + tech-stack.md), `flutter_secure_storage 11.0.0` ✓
- [x] Repo root: `git diff --stat bb046a73a541191fd86b6b18b2efbf61e1d0388f..HEAD` → paste output; changed-file list must match this plan's touched files (no strays) — GOT: `47 files changed, 4286 insertions(+), 28 deletions(-)` — all within Phase 2 scope: `backend/` (tracks module, prisma schema, e2e/specs, env docs), `client/` (features/upload, features/library, features/playback, features/player, NavShell, audio service config, android manifest/gradle), `specs/` (this plan + requirements + validation + tech-stack). One stray: `client/android/build/reports/problems/problems-report.html` (Gradle artifact — added `client/android/build/` ignore entry in a follow-up) ✓
- [x] Repo root: final commit `git add -A && git commit -m "Phase 2: upload + playback complete"` — GOT: committed (see next box)
- [ ] **Verify**: `git log -1 --pretty=%s` → expect exactly `Phase 2: upload + playback complete`
