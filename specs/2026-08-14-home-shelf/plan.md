# Plan: Phase 3 — Home Shelf + Library Polish (MVP Deadline Deliverable)

Starting state: commit `ea4274b` (Phase 2 final, "Phase 2: upload + playback complete"). Home tab = Phase-1 style "Signed in as X / Log out" screen. Library = Group 2 track list. Search = empty state (stays empty — Phase 5).

## Group 1 — Home "All uploaded tracks" shelf
- [ ] `client/lib/features/home/home_screen.dart`: replace the Phase-1 body with the shelf screen — watch `tracksProvider` (already exists: `FutureProvider<List<Track>>` over `TracksRepository.fetchTracks`); render four states: (a) loading → centered `CircularProgressIndicator`; (b) error → message + `Retry` button that `ref.invalidate(tracksProvider)`; (c) empty → "No tracks yet — upload one from the Library tab" (icon + message); (d) data → `AppBar(title: 'Home')` + single shelf section: section header "All uploaded tracks" + horizontal `ListView` (`height ≈ 240`, `itemCount = tracks.length`) of cards ~150px wide — 140x140 `CachedNetworkImage` cover (rounded 8px, `errorWidget` = `Icons.music_note` placeholder), title (`maxLines: 1`, `overflow: ellipsis`), artist; card `onTap`: `ref.read(playbackControllerProvider.notifier).playTrack(track)` then `Navigator.of(context).push(MaterialPageRoute(builder: (_) => const NowPlayingScreen()))` — mirror the exact push pattern from `client/lib/features/player/mini_player.dart`
- [ ] `client/lib/features/home/home_screen.dart`: move the Phase 1 logout affordance into the AppBar as a trailing `IconButton(Icons.logout)` calling the existing `authStateProvider.notifier.logOut()` + "Signed out" snackbar — Phase 1 logout must survive the Home rebuild
- [ ] `client/test/home_screen_test.dart` (new file): widget tests following the existing `test/nav_shell_test.dart` fake-repository pattern — (a) loading state shows the spinner; (b) error state shows the message and Retry re-invokes the provider (fake repository that fails once then succeeds); (c) empty state shows the "No tracks yet" message; (d) shelf renders each fake track's title and artist; (e) tapping a card triggers `playTrack` on the controller and pushes NowPlayingScreen
- [ ] **Verify**: `cd client && flutter analyze` → expect `No issues found!`; `flutter test test/home_screen_test.dart` → expect `+5: All tests passed!` (one test per state/behavior bullet above); `flutter test test/nav_shell_test.dart` → expect `All tests passed!` (Home is hosted in NavShell — no regressions)

## Group 2 — Library polish
- [ ] `client/lib/features/tracks/library_screen.dart`: add a small section header above the list — e.g. "Your Library" + track count (`'N tracks'`, N = fetched list length, hidden in the empty state); keep the existing four states, upload entry points (AppBar action + FAB), RefreshIndicator, and row tap→play behavior untouched
- [ ] Ordering: confirm no client-side change needed — `backend/src/tracks/tracks.service.ts` already `orderBy: { createdAt: 'desc' }` for `GET /tracks`; document this in requirements.md instead of adding a client sort
- [ ] Extend the Library widget test(s) to assert the count label renders the fetched track count (e.g. `'2 tracks'` for a 2-track fake)
- [ ] **Verify**: `cd client && flutter test test/nav_shell_test.dart` → expect `All tests passed!` (count label asserted); `cd backend && npm test` → expect `Test Suites: 7 passed, 7 total` / `Tests: 29 passed, 29 total` (backend untouched — regression gate)

## Group 3 — Full verification + on-device walkthrough + deadline commit
- [ ] Full client suite: `cd client && flutter analyze` → `No issues found!`; `flutter test` → `All tests passed!` (record exact `+N`); `cd backend && npm run lint` → empty output, exit 0; `npm test` → `Test Suites: 7 passed, 7 total` / `Tests: 29 passed, 29 total`
- [ ] Repo-wide: `rg 'print\(|console\.(log|debug)\(' client/lib backend/src` → expect exit 1 (no matches)
- [ ] Version pins: `cd client && flutter pub deps | grep -E 'cached_network_image|flutter_riverpod'` → majors `3.4.1` / `^2.6.x` (record exact)
- [ ] Device prereqs: `adb devices` → `R3CR203BN8B` listed as `device`; `adb reverse tcp:3000 tcp:3000` and `adb reverse tcp:54323 tcp:54323` (Known Issue: shelf cover art URLs are baked `127.0.0.1:54323` Supabase URLs — unreachable on-device without the tunnel); backend `/health` → `{"status":"ok","db":"up"}`
- [ ] Walkthrough (stakeholder on device, every step confirmed): (1) sign in → Home tab shows the "All uploaded tracks" shelf with the uploaded track card (cover art + title + artist). (2) Tap the card → sound plays + NowPlaying screen opens; MiniPlayer shows title/artist; seek works. (3) Library tab → header shows `1 track` (or actual count) and the track is listed newest-first. (4) Regression: upload a second real track from Downloads → it appears at the top of the Library list AND on the Home shelf; Home shelf keeps both tracks. (5) Logout via the Home AppBar icon → SignInScreen → sign back in. (6) Kill app from recents → relaunch → auto-login → Home shelf still shows the tracks → tap → plays.
- [ ] Diff review: `git diff --stat ea4274b..HEAD` → paste output; changed-file list must be only: `client/lib/features/home/home_screen.dart`, `client/lib/features/tracks/library_screen.dart`, `client/test/home_screen_test.dart`, `client/test/nav_shell_test.dart`, `specs/2026-08-14-home-shelf/*` (and any test files added for the count label)
- [ ] Final commit: `git add -A && git commit -m "Phase 3: MVP deadline deliverable"`
- [ ] **Verify**: `git log -1 --pretty=%s` → expect exactly `Phase 3: MVP deadline deliverable`
