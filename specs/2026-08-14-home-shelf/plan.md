# Plan: Phase 3 — Home Shelf + Library Polish (MVP Deadline Deliverable)

Starting state: commit `ea4274b` (Phase 2 final, "Phase 2: upload + playback complete"). Home tab = Phase-1 style "Signed in as X / Log out" screen. Library = Group 2 track list. Search = empty state (stays empty — Phase 5). Visual reference: `specs/references/spotify-screenshots/` + stakeholder's written breakdown in requirements.md (black bg `#000000`, gray `#B3B3B3` secondary text, green `#1DB954` accents, ~8px card radius).

## Group 1 — Home "All uploaded tracks" shelf
- [ ] `client/lib/features/home/home_screen.dart`: replace the Phase-1 body with the shelf screen — watch `tracksProvider` (exists: `FutureProvider<List<Track>>` over `TracksRepository.fetchTracks`); render four states: (a) loading → centered `CircularProgressIndicator`; (b) error → message + `Retry` button that `ref.invalidate(tracksProvider)`; (c) empty → "No tracks yet — upload one from the Library tab" (icon + message); (d) data → `AppBar` with `title: 'Home'` + a leading avatar circle (first initial of the signed-in email from `authStateProvider`, white on a dark gray circle) and no other actions; body = single shelf section: bold white header "All uploaded tracks" (left-aligned, `titleLarge`-style) + horizontal `ListView` (`height ≈ 210`, `itemCount = tracks.length`) of 160px-wide cards: 160x160 `CachedNetworkImage` cover (rounded 8px; `errorWidget` = `Icon(Icons.music_note)` on dark gray), white bold title (`maxLines: 1`, `overflow: ellipsis`, ~13px), gray `#B3B3B3` artist (`maxLines: 1`, ~12px)
- [ ] `client/lib/features/home/home_screen.dart`: avatar tap → small popup menu (e.g. `PopupMenuButton` wrapping the avatar) with a single "Log out" entry calling the existing `authStateProvider.notifier.logOut()` + "Signed out" snackbar — Phase 1 logout must survive the Home rebuild (real-Spotify placement: avatar → menu)
- [ ] `client/lib/features/home/home_screen.dart`: card `onTap`: `ref.read(playbackControllerProvider.notifier).playTrack(track)` then `Navigator.of(context).push(MaterialPageRoute(builder: (_) => const NowPlayingScreen()))` — mirror the exact push pattern from `client/lib/features/player/mini_player.dart`
- [ ] `client/test/home_screen_test.dart` (new file): widget tests following the existing `test/nav_shell_test.dart` fake-repository pattern — (a) loading state shows the spinner; (b) error state shows the message and Retry re-invokes the provider (fake repository that fails once then succeeds); (c) empty state shows the "No tracks yet" message; (d) shelf renders each fake track's title and artist; (e) tapping a card triggers `playTrack` on the controller and pushes NowPlayingScreen
- [ ] **Verify**: `cd client && flutter analyze` → expect `No issues found!`; `flutter test test/home_screen_test.dart` → expect `+5: All tests passed!` (one test per bullet above); `flutter test test/nav_shell_test.dart` → expect `All tests passed!` (Home is hosted in NavShell — no regressions)

## Group 2 — Library polish (real-UI layout)
- [ ] `client/lib/features/tracks/library_screen.dart`: `AppBar` title → `Your Library` (real-UI wording); keep the existing upload entry points (AppBar action + FAB) and the four states + RefreshIndicator + row tap→play behavior untouched
- [ ] `client/lib/features/tracks/library_screen.dart`: add a sort row under the AppBar — left-aligned "↓↑ Recents" chip that toggles `Recents ↔ A–Z` on each tap: Recents = backend order (already `createdAt desc` — no fetch change); A–Z = client-side `title` sort (case-insensitive) of the fetched list; toggle is local widget state (e.g. `StatefulWidget` or a local `ValueNotifier`); active-state label shows the current mode
- [ ] `client/lib/features/tracks/library_screen.dart`: restyle rows to the real-UI anatomy — ~56px square cover thumb (rounded 8px, `CachedNetworkImage`, `music_note` errorWidget), white bold title (`maxLines: 1`), gray `#B3B3B3` subtitle: `'Album • $artist'` when `track.album != null` else `'Single • $artist'` (`maxLines: 1`) — no download/pin indicators (not applicable to our catalog — omitted, not faked)
- [ ] Ordering note: confirm no backend change — `backend/src/tracks/tracks.service.ts` already `orderBy: { createdAt: 'desc' }`; document in requirements.md (done) instead of adding a server sort
- [ ] Extend the Library widget tests (in `client/test/nav_shell_test.dart` or the library-related tests): (a) subtitle format — track with album renders `Album • artist`, without album renders `Single • artist`; (b) sort toggle — tap "↓↑ Recents" → list reorders A–Z (assert first title), tap again → back to newest-first
- [ ] **Verify**: `cd client && flutter test test/nav_shell_test.dart` → expect `All tests passed!` (subtitle + toggle asserted); `cd backend && npm test` → expect `Test Suites: 7 passed, 7 total` / `Tests: 29 passed, 29 total` (backend untouched — regression gate)

## Group 3 — Full verification + on-device walkthrough + deadline commit
- [ ] Full client suite: `cd client && flutter analyze` → `No issues found!`; `flutter test` → `All tests passed!` (record exact `+N`); `cd backend && npm run lint` → empty output, exit 0; `npm test` → `Test Suites: 7 passed, 7 total` / `Tests: 29 passed, 29 total`
- [ ] Repo-wide: `rg 'print\(|console\.(log|debug)\(' client/lib backend/src` → expect exit 1 (no matches)
- [ ] Version pins: `cd client && flutter pub deps | grep -E 'cached_network_image|flutter_riverpod'` → majors `3.4.1` / `^2.6.x` (record exact)
- [ ] Device prereqs: `adb devices` → `R3CR203BN8B` listed as `device`; `adb reverse tcp:3000 tcp:3000` and `adb reverse tcp:54323 tcp:54323` (Known Issue: shelf cover art URLs are baked `127.0.0.1:54323` Supabase URLs — unreachable on-device without the tunnel); backend `/health` → `{"status":"ok","db":"up"}`
- [ ] Walkthrough (stakeholder on device, every step confirmed): (1) sign in → Home tab shows the avatar circle top-left and the "All uploaded tracks" shelf with the uploaded track card (cover art + bold title + gray artist). (2) Tap the card → sound plays + Now Playing screen opens; MiniPlayer shows title/artist; seek works. (3) Library tab → "Your Library" header; the track row shows the cover thumb, title, and `Single • Famous Pluto ft Brazy`-style subtitle; "↓↑ Recents" active. (4) Tap the sort chip → list reorders A–Z (title order); tap again → newest-first. (5) Regression: upload a second real track from Downloads → it appears at the top of the Library list (Recents) AND on the Home shelf. (6) Avatar tap → menu → Log out → SignInScreen → sign back in. (7) Kill app from recents → relaunch → auto-login → Home shelf still shows the tracks → tap → plays.
- [ ] Diff review: `git diff --stat ea4274b..HEAD` → paste output; changed-file list must be only: `client/lib/features/home/home_screen.dart`, `client/lib/features/tracks/library_screen.dart`, `client/test/home_screen_test.dart`, `client/test/nav_shell_test.dart`, `specs/references/spotify-screenshots/*`, `specs/2026-08-14-home-shelf/*` (and any test files added for Library assertions)
- [ ] Final commit: `git add -A && git commit -m "Phase 3: MVP deadline deliverable"`
- [ ] **Verify**: `git log -1 --pretty=%s` → expect exactly `Phase 3: MVP deadline deliverable`