# Plan: Phase 1 — Auth (incl. Phase 0 groundwork)

> Execution note (2026-08-13): dev database pivoted to local Supabase CLI instance per stakeholder decision (see tech-stack.md change log). Group 1 Supabase tasks were executed against the local instance: `supabase init` + offset ports in `supabase/config.toml` (api 54323, db 54324, studio 54325, smtp 54326) + `supabase start --ignore-health-check` (health checks unreliable on this host — Known Issue in tech-stack.md). DB URL: `postgresql://postgres:postgres@127.0.0.1:54324/postgres`. Resend API key provided by stakeholder.

Sequential groups. No group starts until the previous group's Verify passes.

## Group 1 — Repo baseline + Supabase project
- [x] Repo root: `git init`, then create `.gitignore` at repo root covering `node_modules/`, `.env`, `backend/dist/`, `client/build/`, `.dart_tool/`, `*.iml`, `.idea/`, `.vscode/` — GOT: `git init` ok; .gitignore written (also covers `supabase/.branches`, `supabase/.temp`, `supabase/.env`)
- [x] Repo root: commit everything as the rollback baseline: `git add -A && git commit -m "baseline: specs/"` — GOT: `[master (root-commit) 7b23dff] baseline: specs/ 9 files changed, 358 insertions(+)`
- [x] Browser (user's account): create Supabase free-tier project named `spotify-clone` at https://supabase.com; from Project Settings → Database, copy the **direct** connection string (port 5432, host `db.<ref>.supabase.co`) — do NOT use the pooler port 6543 (see tech-stack.md Known Issues, carried into requirements.md) — GOT: superseded by stakeholder decision — local Supabase CLI instance instead. `supabase init` + `supabase/config.toml` port offsets (api 54323, db 54324, studio 54325, smtp 54326); `supabase start --ignore-health-check` → running; DB URL `postgresql://postgres:postgres@127.0.0.1:54324/postgres` (see tech-stack.md change log + Known Issues)
- [x] Browser (user's account): create Resend account at https://resend.com; create API key; confirm the account-owner email is verified in the sandbox (see Known Issue in requirements.md) — GOT: API key provided by stakeholder (`re_Pq...XdP`); owner email still needed for Group 6 live delivery check — requested again there
- [x] Browser: generate the JWT signing secret locally: `openssl rand -base64 64` → record it for Group 2's `backend/.env` — GOT: generated, stored at /tmp/opencode/jwt-secret (90 bytes), env-only
- [x] **Verify**: `git log -1 --pretty=%s` → expect exactly `baseline: specs/` (and `git status --porcelain` → expect empty output) — GOT: `baseline: specs/` ✅ and `git status --porcelain` → empty (0 lines) ✅

## Group 2 — NestJS skeleton + Prisma schema + health check
- [x] Repo root: scaffold backend: `npx @nestjs/cli new backend --package-manager npm --skip-git --strict` — GOT: scaffolded (initial `npm install` interrupted once by user, re-run to completion)
- [x] `backend/`: install deps: `npm i @nestjs/config @nestjs/jwt @nestjs/passport passport passport-jwt bcryptjs google-auth-library resend` and `npm i -D prisma` then `npm i @prisma/client` — GOT: installed. NOTE: first install pulled Prisma 7.9.1 (CLI 7) which broke `url = env()` in schema — downgraded to `prisma@^6` → 6.19.3, matching tech-stack pin
- [x] `backend/`: create `.env.example` and `.env` with keys: `DATABASE_URL` (Supabase direct URI from Group 1), `JWT_SECRET` (from Group 1), `ACCESS_TOKEN_TTL=15m`, `REFRESH_TOKEN_TTL=30d`, `RESET_TOKEN_TTL=15m`, `RESEND_API_KEY`, `RESEND_FROM=onboarding@resend.dev`, `GOOGLE_CLIENT_ID` (empty for now, filled in Group 7), `PORT=3000`; `.env` is gitignored, `.env.example` is committed with no secret values — GOT: `.env.example` committed; `.env` written with local Supabase `DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54324/postgres`, generated `JWT_SECRET`, stakeholder's `RESEND_API_KEY` (gitignored)
- [x] `backend/prisma/schema.prisma`: write the Phase 0 schema — `User` (id cuid, email unique, passwordHash nullable, displayName nullable, googleSub unique nullable, avatarUrl nullable, timestamps), `Track`, `Playlist`, `PlaylistTrack` (position, @@unique[playlistId, trackId]), `LikedTrack` (@@unique[userId, trackId]) with relations matching roadmap.md Phase 0 — GOT: written (5 models, relations with onDelete: Cascade)
- [x] `backend/`: run `npx prisma migrate dev --name init` (applies migration to Supabase) and `npx prisma generate` — GOT: `20260813090020_init` applied; `npx prisma migrate status` → `Database schema is up to date!`; Prisma Client v6.19.3 generated
- [x] `backend/src/prisma/`: `PrismaModule` (global) + `PrismaService` extending `PrismaClient`; wire `ConfigModule.forRoot({isGlobal: true, validationSchema})` with Joi-free manual validation of required env keys (fail fast at boot) — GOT: PrismaModule/PrismaService written; ConfigModule validates REQUIRED_ENV presence, throws at boot on missing key; boilerplate AppController/AppService removed
- [x] `backend/src/health/health.controller.ts`: `GET /health` runs `SELECT 1` via Prisma and returns the exact JSON `{"status":"ok","db":"up"}` — GOT: written
- [x] **Verify**: start server with `npm run start:dev` (wait for log line `Nest application successfully started`), then `curl -s http://localhost:3000/health` → expect exactly `{"status":"ok","db":"up"}` — GOT: log line `[NestApplication] Nest application successfully started` ✅; `curl -s http://localhost:3000/health` → `{"status":"ok","db":"up"}` ✅ (also confirms Prisma ↔ local Supabase Postgres connectivity)

## Group 3 — Server-side auth: signup / login / refresh / me / password-reset endpoints
- [x] `backend/`: delete the Nest boilerplate unit specs `src/app.controller.spec.ts` and `src/app.service.spec.ts` (they test scaffolding the health controller replaces) — GOT: deleted; also removed unused AppController/AppService
- [x] `backend/src/users/`: `users.module.ts` + `users.service.ts` (`findByEmail`, `findById`, `create`, `upsertGoogle`, `updatePassword`) + `users.service.spec.ts` — GOT: written; 5 unit tests (hit/miss/create/upsert) — one more than drafted (upsertGoogle warranted its own test); plan/validation totals updated to 19
- [x] `backend/src/auth/password.service.ts`: bcryptjs `hash` (cost 10) + `verify` + `password.service.spec.ts` with 3 unit tests (roundtrip true, wrong password false, hashes differ per password) — GOT: written, 3 tests
- [x] `backend/src/auth/reset-token.service.ts`: mint/verify a purpose-scoped JWT (claim `purpose: "password_reset"`, TTL from `RESET_TOKEN_TTL`) + `reset-token.service.spec.ts` with 4 unit tests (mint, valid verify, wrong-purpose rejected, expired rejected) — GOT: written, 4 tests
- [x] `backend/src/auth/auth.service.ts` + `auth.controller.ts`: endpoints — `POST /auth/signup` {email, password, displayName?} → 201 {user, accessToken, refreshToken} (409 on duplicate email), `POST /auth/login` → 200 same shape (401 on bad credentials), `POST /auth/refresh` {refreshToken} → 200 new pair (rotation, 401 on invalid), `POST /auth/forgot-password` {email} → 202 always (both known and unknown emails, no user enumeration), `POST /auth/reset-password` {token, newPassword} → 200 (401 on invalid/expired token), `GET /auth/me` → 200 user; access token 15m / refresh 30d signed with `JWT_SECRET` — GOT: written; tokens carry `jti: randomUUID()` (see Verify attempt 2); `expiresIn` cast via `SignOptions['expiresIn']` (TS strict wants the `ms` StringValue type)
- [x] `backend/src/auth/`: `jwt.strategy.ts` (passport-jwt from `Authorization: Bearer`) + `jwt-auth.guard.ts` + `jwt.strategy.spec.ts` with 2 unit tests — GOT: written; added devDeps `@types/passport` + `@types/passport-jwt` (TS7016 fix)
- [x] `backend/src/auth/auth.service.spec.ts`: 5 unit tests with mocked collaborators (test doubles, test files only) — signup hashes+returns pair, duplicate → conflict, login ok, login wrong → unauthorized, refresh rotates — GOT: written, 5 tests (real JwtService with test secret)
- [x] `backend/src/auth/auth.controller.ts`: forgot-password sends email via `resend` SDK (`From: onboarding@resend.dev`, `To: <requested email>`, body contains the reset link); no fallback logging of the token anywhere — GOT: email link is `spotifyclone://auth/reset?token=<t>` (custom scheme so tapping opens the app — matches Group 6's adb deep link; the drafted `https://spotifyclone.auth` URL would not open the app, intentional correction); delivery failures → `Logger.warn` without the token, endpoint still 202
- [x] `backend/test/auth.e2e-spec.ts` (replaces `test/app.e2e-spec.ts`): supertest against the running app with real Supabase, unique emails per run (`e2e-${Date.now()}@test.local`), cleanup after run — exactly 10 tests: (1) signup → 201 with user+tokens, (2) duplicate signup → 409, (3) login → 200 tokens, (4) login wrong password → 401, (5) `/auth/me` with access token → 200, (6) `/auth/me` without token → 401, (7) refresh → 200 new pair and new access token works on `/auth/me`, (8) refresh with garbage → 401, (9) forgot-password → 202 (known and unknown email), (10) reset-password with service-minted token → 200 then login with the new password → 200 — GOT: written, 10 tests; `afterAll` deletes `e2e-%@test.local` users
- [x] **Verify**: `npm run test:e2e` → expect `Test Suites: 1 passed, 1 total` and `Tests: 10 passed, 10 total`; also `npm test` → expect `Test Suites: 5 passed, 5 total` and `Tests: 19 passed, 19 total` — Verify attempt 1: FAILED — all 10 e2e: `TypeError: request is not a function` — Diagnosis: supertest 7.x exports a default function; `import * as request` no longer works — Fix applied: `import request from 'supertest'` (esModuleInterop on). Verify attempt 2: FAILED — 1 of 10: refreshed.refreshToken EQUAL to the original (both signed in the same wall-clock second → identical iat/payload → identical JWT) — Diagnosis: stateless rotation needs a uniqueness claim — Fix applied: `jti: randomUUID()` on access + refresh tokens. Verify attempt 3: PASSED — `Test Suites: 1 passed, 1 total` / `Tests: 10 passed, 10 total` ✅; `npm test` → `Test Suites: 5 passed, 5 total` / `Tests: 19 passed, 19 total` ✅

## Group 4 — Flutter app bootstrap + auth infrastructure
- [ ] Repo root: `flutter create client --org com.spotifyclone --project-name spotify_clone --platforms android,ios`
- [ ] `client/pubspec.yaml`: add `flutter_riverpod ^2.6.x`, `flutter_secure_storage ^9.x`, `dio ^4.x`, `google_fonts ^6.x`, `google_sign_in ^6.x` (wired in Group 7); run `flutter pub get`
- [ ] `client/lib/theme/`: theme file with token colors (background `#121212`, surface `#181818`, primary `#1DB954`, text `#FFFFFF`/`#B3B3B3`) and Inter from google_fonts; apply in `client/lib/main.dart`
- [ ] `client/lib/core/api/api_client.dart`: dio instance, base URL from `--dart-define=API_BASE_URL` (default `http://10.0.2.2:3000` for the Android emulator; for a physical phone run `adb reverse tcp:3000 tcp:3000` once and pass `--dart-define=API_BASE_URL=http://127.0.0.1:3000`), interceptor attaching stored access token to `Authorization: Bearer`; refresh-on-401 interceptor: exactly one retry via `POST /auth/refresh`, on refresh failure clear tokens and emit logged-out state
- [ ] `client/lib/core/auth/`: `auth_repository.dart` (dio calls + `flutter_secure_storage` for `accessToken`/`refreshToken`), `auth_notifier.dart` (Riverpod StateNotifier: `AuthState {unknown, authenticated(user), unauthenticated}`; `signUp`, `logIn`, `logOut`, `restoreSession`, `resetPassword`), `auth_providers.dart`
- [ ] `client/lib/`: auto-login on startup — `restoreSession()` on app boot: read stored access token, validate via `GET /auth/me` (401 → refresh once); while `AuthState.unknown`, show a branded splash
- [ ] `client/lib/features/home/home_screen.dart`: post-auth landing showing `Signed in as <email>` and a `Log out` button calling `logOut()` (clears secure storage → back to SignInScreen); no nav shell yet (deferred to Phase 2)
- [ ] `client/android/app/src/main/AndroidManifest.xml`: add `android:usesCleartextTraffic="true"` only in `client/android/app/src/debug/AndroidManifest.xml` overlay (never in release)
- [ ] `client/test/`: widget/Dart tests with test doubles (mock dio adapter + mocked secure-storage plugin) — exactly 7 tests: auth_notifier 4 (starts unauthenticated, login success → authenticated, login failure → stays unauthenticated, logout → unauthenticated), interceptor 3 (attaches token, refreshes on 401 and retries once, logs out on refresh failure)
- [ ] **Verify**: `flutter analyze` → expect `No issues found!`; then `flutter test` → expect `All tests passed!`

## Group 5 — Sign-in / Sign-up screens + on-device walkthrough
- [ ] `client/lib/features/auth/`: `sign_in_screen.dart` (fields Email, Password; buttons `Log in`, `Continue with Google` (disabled until Group 7), links `Sign up` and `Forgot password?`), `sign_up_screen.dart` (fields Email, Password, Confirm password, Display name; button `Create account`), form validation (valid email, password ≥ 8 chars, passwords match), real API calls through `AuthNotifier` — no mocked paths
- [ ] `client/lib/main.dart`: route SignInScreen/SignUpScreen/HomeScreen by `AuthState` (Riverpod `ref.watch`), with `go`-free named routes; error states surface the server message (e.g. `401` → "Invalid email or password")
- [ ] **Verify** (manual, literal): with the phone connected via USB debugging and the Group 2 backend running: (0) precheck: `adb devices` → the phone's serial listed as `device`; `adb reverse tcp:3000 tcp:3000` → outputs `3000` (or nothing, exit 0). (1) `flutter run -d <phone-serial> --dart-define=API_BASE_URL=http://127.0.0.1:3000`; expect Splash → SignInScreen. (2) Tap `Sign up`, enter `phase1-walkthrough@test.local` / `Password123!` / display name `Walkthrough`, tap `Create account` → expect HomeScreen showing `Signed in as phase1-walkthrough@test.local`. (3) Tap `Log out` → expect SignInScreen. (4) Log in with the same credentials → expect HomeScreen again. (5) Kill the app from recents, relaunch from the launcher → expect HomeScreen directly (auto-login), NOT SignInScreen.

## Group 6 — Password reset UI + deep link
- [ ] `client/lib/features/auth/forgot_password_screen.dart`: Email field + `Send reset link` button → calls `POST /auth/forgot-password`, then shows exactly `Check your email` regardless of account existence
- [ ] `client/lib/features/auth/reset_password_screen.dart`: reads the reset token from the app's launch route (custom scheme `spotifyclone://auth/reset?token=...` via `WidgetsBinding.instance.platformDispatcher.defaultRouteName`, cold-start only), shows New password + Confirm password fields + `Reset password` button → `POST /auth/reset-password` → on 200 navigate to SignInScreen with a success message
- [ ] `client/android/app/src/main/AndroidManifest.xml`: add intent-filter on `MainActivity` — `ACTION_VIEW` + `BROWSABLE` + data `spotifyclone://auth/reset`
- [ ] **Verify** (manual, literal): with backend running and the app installed on the phone via `flutter run`: (1) `curl -s -X POST http://localhost:3000/auth/forgot-password -H 'Content-Type: application/json' -d '{"email":"<resend-owner-email from Group 1>"}' -w '\n%{http_code}'` → expect `202`. (2) Open https://resend.com/emails, confirm the delivered email, copy `token=...` from the link (if the phone has a mail app signed into the owner account, open the link there directly instead — same expected result). (3) `adb shell am start -a android.intent.action.VIEW -d "spotifyclone://auth/reset?token=<copied-token>"` → expect the app opens directly on ResetPasswordScreen. (4) Enter `NewPassword456!` twice, tap `Reset password` → expect SignInScreen with success message. (5) Log in with `phase1-walkthrough@test.local` / `NewPassword456!` → expect HomeScreen.

## Group 7 — Google OAuth + final walkthrough + hardening
- [ ] Browser (user's Google account, https://console.cloud.google.com): create OAuth 2.0 Client IDs in one project — **Android** (package `com.spotifyclone.spotify_clone`, SHA-1 from `keytool -list -v -keystore ~/.android/debug.keystore -alias androiddebugkey -storepass android -keypass android 2>/dev/null | grep SHA1`), **iOS** (bundle id `com.spotifyclone.spotifyClone`), **Web** (no redirect URI needed for the native flow)
- [ ] `backend/.env`: set `GOOGLE_CLIENT_ID=<web client id>` (this is the audience `google_sign_in` signs the ID token for)
- [ ] `backend/src/auth/google-auth.service.ts`: `google-auth-library` `OAuth2Client.verifyIdToken({idToken, audience: GOOGLE_CLIENT_ID})` → upsert User via `usersService.upsertGoogle` (match on `googleSub`, else on email, set `googleSub` + `avatarUrl`) → issue token pair; endpoint `POST /auth/google` {idToken} → 200 {user, accessToken, refreshToken}, 400 missing token, 401 unverifiable token
- [ ] `backend/test/google.e2e-spec.ts`: exactly 2 tests — missing idToken → 400, malformed idToken → 401
- [ ] `client/lib/features/auth/sign_in_screen.dart`: wire `Continue with Google` — `google_sign_in` with `serverClientId: GOOGLE_CLIENT_ID`, send returned idToken to `POST /auth/google`, on success store tokens and emit authenticated; real Google account required (no mocked path)
- [ ] Repo-wide: remove any debug `print`/`console.log` left in `client/` and `backend/` (none should exist per engineering-standards; grep to confirm)
- [ ] Repo root: final commit `git add -A && git commit -m "Phase 1: auth complete"`
- [ ] **Verify**: (1) `cd backend && npm run lint && npm test && npm run test:e2e` → expect lint exits 0 with empty output, `Test Suites: 5 passed, 5 total` / `Tests: 19 passed, 19 total`, `Test Suites: 2 passed, 2 total` / `Tests: 12 passed, 12 total`. (2) `cd ../client && flutter analyze && flutter test` → `No issues found!` and `All tests passed!`. (3) Manual walkthrough, literal (on the phone, USB-connected): on the SignInScreen tap `Continue with Google` → Google account chooser → select an account → consent → expect HomeScreen showing the Google account's email; then relaunch the app → expect HomeScreen (auto-login persists). (4) Full reset flow re-run with a real fresh password per Group 6 steps. (5) `git log -1 --pretty=%s` → `Phase 1: auth complete`.
